apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Reference the default base configuration
resources:
  - ../../default

# Patch to configure production-ready settings
patches:
  - path: manager_production_patch.yaml
    target:
      kind: Deployment
      name: controller-manager
